package com.html;


public class HtmlUtil {
    /*
    
    public static String singleTagFormat = "^<.*>$";
    public static String doubleTagFormat = "^<.*>.*<.*>$";
    
    public static Boolean isTag(String target) {
 
        if(target.startsWith("<") && target.endsWith(">")) {
            return true;
        }
        return false;
    }
    
    public static Boolean isSingleTag(String target) {
        if(isTag(target) && !isDoubleTag(target) ) {
            return true;
        }
        return false;
    }
    
    public static Boolean isDoubleTag(String target) {
        return target.matches(doubleTagFormat);
    }
    */
}
